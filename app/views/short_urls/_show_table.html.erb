<% local_assigns[:clicks].each do |click| %>
  <tr>
    <td class="text-start"><%= sanitize_report_content click.coordinates %></td>
    <td class="text-start"><%= sanitize_report_content click.locality %></td>
    <td><%= click.created_at %></td>
  </tr>
<% end %>
